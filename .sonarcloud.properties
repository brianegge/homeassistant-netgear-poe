# SonarCloud automatic-analysis configuration.
# Classify the pytest suite as test code so fixture literals (mock IPs,
# mock http:// URLs) don't count against the production maintainability
# and security ratings.
sonar.sources=custom_components
sonar.tests=tests
